'use strict'
console.log("Ex 24 Solution!!!");

// 24.+UnitTesting Play with the function Math.abs(), read the documentation in MDN. 
// Implement myAbs(), write the function yourself.

console.log(myAbs(-5));
function myAbs(x) {
    if (x < 0)
        return x * -1;
    return x
}